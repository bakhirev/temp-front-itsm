import styled from 'styled-components';

import { Body2Long } from '../../Typography';

export const Caption13 = styled(Body2Long)`
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;
