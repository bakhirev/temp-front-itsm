import styled from 'styled-components';

import { Body1Long } from '../../../Typography';

export const OptionText = styled(Body1Long)`
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;
