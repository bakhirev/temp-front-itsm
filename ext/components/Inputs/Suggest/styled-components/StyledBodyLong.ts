import styled from 'styled-components';

import { Body1Long, Body2Long } from '../../../Typography';

export const StyledBody1Long = styled(Body1Long)`
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;

export const StyledBody2Long = styled(Body2Long)`
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;
